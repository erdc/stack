extends: [autotools_package]
dependencies:
  build: [readline]

sources:
- key: tar.gz:n6hgu6ztkjzxjcaw7gz3mk55ynzkrco6
  url: https://sqlite.org/2024/sqlite-autoconf-3460000.tar.gz

defaults:
  # /bin/h5pcc contains hard-coded path
  relocatable: false

build_stages:
- name: configure
  after: prologue
  extra: ['--with-pic']

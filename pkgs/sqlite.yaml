extends: [autotools_package]
dependencies:
  build: [readline]

sources:
- key: tar.gz:llyh32mcxjsy7wi2amlqzfc7thexd5uv
  url: http://sqlite.org/2022/sqlite-autoconf-3380500.tar.gz

defaults:
  # /bin/h5pcc contains hard-coded path
  relocatable: false

build_stages:
- name: configure
  after: prologue
  extra: ['--with-pic','--disable-shared']

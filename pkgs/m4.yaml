extends: [autotools_package]
dependencies:
  build: []

defaults:
  # /bin/m4 contains hard-coded path
  relocatable: false

sources:
- key: tar.xz:moxn4xdngo3ntmjvchgqxywkybdpfzyp
  url: http://ftp.gnu.org/gnu/m4/m4-1.4.19.tar.xz

when_build_dependency:
  - prepend_path: PATH
    value: '${ARTIFACT}/bin'

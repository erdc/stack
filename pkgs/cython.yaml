extends: [setuptools_package]

dependencies:
  build: []
  run: []

sources:
- key: tar.gz:6hahqsg6nqn35gk4fpn6en7fld6qq5dv
  url: https://github.com/cython/cython/archive/0.29.33.tar.gz

when_build_dependency:
  - prepend_path: PATH
    value: ${ARTIFACT}/bin
  - prepend_path: PYTHONPATH
    value: '${ARTIFACT}/lib/python{{pyver}}/site-packages'

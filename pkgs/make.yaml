extends: [autotools_package]

defaults:
  relocatable: false

sources:
- key: tar.gz:lapu2tuhfwtuwokbzb2ccweyu7jvqaxq
  url: https://ftp.gnu.org/gnu/make/make-4.4.tar.gz

when_build_dependency:
- prepend_path: PATH
  value: '${ARTIFACT}/bin'

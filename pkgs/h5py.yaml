extends: [setuptools_package]

dependencies:
  build: [hdf5, mpi4py, mpi, numpy, cython, pkgconfig, six]
  run: [numpy, mpi4py, pkgconfig, cython]

sources:
- key: tar.gz:anhmehzi6lzo3rcueuk2tlsfgvvclo2f
  url: https://github.com/h5py/h5py/archive/refs/tags/3.11.0.tar.gz

build_stages:
- name: install
  after: setup_dirs
  mode: replace
  handler: bash
  bash: |
       HDF5_VERSION=1.14.3 HDF5_MPI="ON"  \
       CC=${MPICC} CXX=${MPICXX} ${PYTHON} -c 'import setuptools; __file__="setup.py"; exec(open(__file__).read())' \
       ${SETUPTOOLS_PACKAGE_EXTRA_OPTIONS} \
       install \
       --prefix=. --root=${ARTIFACT} \
       --single-version-externally-managed


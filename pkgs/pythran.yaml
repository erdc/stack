extends: [setuptools_package]

dependencies:
  build: [numpy, blas, beniget, ply, gast]
  run: [numpy, blas, beniget, ply, gast]

sources:
- key: tar.gz:4ck2d2me7d2ez7omxdoxs6xmj44ntcuq
  url: https://github.com/serge-sans-paille/pythran/archive/refs/tags/0.11.0.tar.gz

when_build_dependency:
  - prepend_path: PATH
    value: ${ARTIFACT}/bin
  - prepend_path: PYTHONPATH
    value: '${ARTIFACT}/lib/python{{pyver}}/site-packages'

extends: [autotools_package]
dependencies:
  build: []

sources:
- key: tar.gz:btwlf3ymm6ywnxutomtwtk66xicvkcdn
  url: http://download.oracle.com/berkeley-db/db-18.1.40.tar.gz

build_stages:
- name: configure
  mode: replace
  handler: bash
  bash: |
    mkdir build
    cd build
    ../dist/configure --enable-cxx --prefix=$ARTIFACT
